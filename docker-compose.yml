version: '3.9'

services:
  mongodb:
    image: mongo:7.0
    container_name: artisanalley-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - artisanalley-network

  server:
    build: ./server
    image: artisanalley/mern-server
    container_name: artisanalley-server
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env        
    depends_on:
      - mongodb
    volumes:
      - ./server:/app
      - /app/node_modules
    networks:
      - artisanalley-network

  client:
    build: ./client
    image: artisanalley/mern-client
    container_name: artisanalley-client
    restart: unless-stopped
    ports:
      - "5173:5173"
    env_file:
      - .env
    depends_on:
      - server
    volumes:
      - ./client:/app
      - /app/node_modules
    networks:
      - artisanalley-network

volumes:
  mongodb_data:
    driver: local

networks:
  artisanalley-network:
    driver: bridge
